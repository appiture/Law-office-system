{
  "name": "law-office-demo",
  "version": "1.0.0",
  "description": "Orchestrates the Law Office Demo System with zero-config setup.",
  "scripts": {
    "install:all": "cd backend && mvnw.cmd clean install -DskipTests && cd ../frontend && npm install",
    "dev:backend": "cd backend && mvnw.cmd spring-boot:run",
    "dev:frontend": "cd frontend && npm run dev",
    "demo": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "setup": "npm install && npm run install:all"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
